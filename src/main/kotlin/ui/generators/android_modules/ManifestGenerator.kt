package ui.generators.android_modules

import ui.models.AndroidModuleBlueprint

class ManifestGenerator {

    /**
     * generates manifest by blueprint and list of activity names
     */
    fun generate(blueprint: AndroidModuleBlueprint, activies: List<String>) {
    }
}